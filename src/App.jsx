import MainMenu from './screens/MainMenu.jsx'

export default function App() {
  // TODO: routing. For now the menu reports the chosen action and nothing
  // consumes it — the create/join/profile/settings screens don't exist yet.
  return <MainMenu />
}
