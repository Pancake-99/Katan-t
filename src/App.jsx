export default function App() {
  return (
    <main>
      <h1>Katan</h1>
      {/* Throwaway specimen — confirms all four faces load. Delete when done. */}
      <p style={{ fontFamily: 'var(--font-ui)' }}>Roboto — interface text</p>
      <p style={{ fontFamily: 'var(--font-body)' }}>Open Sans — body text</p>
      <p style={{ fontFamily: 'var(--font-print)', fontSize: '2rem' }}>
        Jacquard 12 — printed components
      </p>
    </main>
  )
}
